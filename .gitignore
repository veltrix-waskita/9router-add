node_modules/
/config.json
.env
*.log
test/fixtures/aliases.txt
/aliases.txt
/generated-accounts-*.json
.batch-stats.json
/proxies.txt
# Backups/variants carry the same user:pass@host credentials — never commit.
/proxies.txt.*
/proxies*.txt
__pycache__/
src/providers/kiro/worker/.venv/
src/providers/grok-cli/worker/.venv/
src/providers/qoder/worker/.venv/
captcha-solver/venv/
captcha-solver/.venv/
captcha-solver/solver.env

# Account output directories — contain live PATs/tokens (0600, appended per success)
# Keep .gitkeep files so the directory structure is tracked
/accounts/*
!/accounts/.gitkeep
!/accounts/*/
!/accounts/*/.gitkeep

# Qoder trial claim tool (vendored — contains encrypted blob + machine creds pool)
/qoder-trial/qoder_creds_10k.json
/qoder-trial/runtime-info-linux-x64
/qoder-trial/hooks/spoof_hw.so

# Sensitive files — never commit
/.qoder/
/qoder.json
/x-farm.zip
/tmp/
docs/superpowers/plans/2026-08-04-pateway-farm-wiring.md

# Per-provider generated credentials + aliases inside accounts/
/accounts/*/generated-accounts-*.json
/accounts/*/aliases.txt

# Account credentials pool — 650 GSuite accounts (sensitive, never commit)
/accounts.json
